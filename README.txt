Dotfiles of @jomag
------------------

Private dotfiles. Should not contain any secrets, but is also not
for public use.

This directory contains dotfiles that managed by the GNU Stow software.
On a new computer, checkout this directory and run `stow .` in it. This
will symlink the dotfiles in this directory to their correct place in
the parent directory.

